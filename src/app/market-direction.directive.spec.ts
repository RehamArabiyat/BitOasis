import { MarketDirectionDirective } from './market-direction.directive';

describe('MarketDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MarketDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
